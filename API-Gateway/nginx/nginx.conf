http{
	upstream flask {  
	  ip_hash;
	  server flask:8080;
	}
	server {  
	  location / {
			proxy_pass http://flask;
		}
		
	  listen 80;
	  server_name nginx;
	}
}
events { worker_connections 1024; }
