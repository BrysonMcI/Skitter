version: '3'
services:
  proxy:
    image: nginx
    volumes:
     - ./nginx/:/etc/nginx/
    ports:
     - "9000:9000"
  puma:
    build: ./ruby
    ports:
     - "9292:9292"
    command: "bundle exec puma"